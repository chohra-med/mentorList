{
  "name": "mentor_list",
  "version": "0.0.2",
  "private": true,
  "author": "CHOHRA Mohammed",
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint .",
    "link-ios": "cd ios/ && pod install && cd .. && yarn ios",
    "clean-android": "cd android/ && ./gradlew clean && cd .. && yarn android",
    "create-apk": "cd android/  && ./gradlew assembleRelease && cd ..",
    "create-aab": "cd android/ && ./gradlew clean && ./gradlew bundleRelease && cd .."
  },
  "dependencies": {
    "lodash": "latest",
    "react": "16.8.3",
    "react-native": "0.59.9",
    "react-native-dotenv": "^0.2.0",
    "react-native-i18n": "^2.0.15",
    "react-native-vector-icons": "^6.6.0"
  },
  "devDependencies": {
    "@babel/core": "7.4.4",
    "@babel/runtime": "7.4.4",
    "babel-jest": "24.8.0",
    "jest": "24.8.0",
    "metro-react-native-babel-preset": "0.54.0",
    "react-test-renderer": "16.8.3"
  },
  "jest": {
    "preset": "react-native"
  }
}
